use proc_macro::TokenStream;
macro_rules! pass{($($n:ident),*)=>{$(#[proc_macro_attribute]pub fn $n(_a:TokenStream,i:TokenStream)->TokenStream{i})*}}
pass!(
    critical,
    critical_math,
    critical_parser,
    critical_serializer,
    critical_crypto,
    critical_state,
    critical_concurrency,
    critical_async,
    critical_alloc,
    critical_layout,
    critical_ffi,
    critical_hw,
    secret,
    hot_path,
    no_alloc,
    must_handle,
    allow,
    test_for,
    test_kind,
    fuzz_required,
    fuzz_target_for,
    property_required,
    property,
    alloc_budget,
    cancel_safe,
    cancel_unsafe,
    state_machine,
    initial,
    terminal,
    roundtrip,
    differential,
    no_panic,
    hot_shared,
    vectorize_expected,
    mmio,
    interrupt,
    dma_buffer,
    arena
);
