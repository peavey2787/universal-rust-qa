# Contributing

Keep modules single-purpose, avoid broad facade layers for trivial helpers, and add pass/fail fixtures for every rule. New QA rule IDs are compatibility surface and require registry documentation.
