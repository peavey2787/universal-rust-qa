# Configuration

Repository-specific policy lives in one `qa.toml`. The interactive settings menu edits the same file. `export-config` and `import-config` copy the complete configuration, including exceptions and viewer settings.
